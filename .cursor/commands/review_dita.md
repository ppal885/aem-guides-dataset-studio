# Review DITA XML

Use the `aem-guides-dataset-studio` MCP server and call the `review_dita_xml` tool.

- Treat everything after `/review_dita` as the XML input.
- If no XML is provided, ask the user to paste the DITA XML to review.
- Summarize score, main issues, and top improvements clearly.
