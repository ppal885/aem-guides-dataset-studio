# Lookup AEM Guides

Use the `aem-guides-dataset-studio` MCP server and call the `lookup_aem_guides` tool.

- Treat everything after `/lookup_aem` as the user query.
- If no query is provided, ask what AEM Guides topic should be searched.
- Prefer a concise grounded answer over raw tool output.
