# Lookup DITA Spec

Use the `aem-guides-dataset-studio` MCP server and call the `lookup_dita_spec` tool.

- Treat everything after `/lookup_dita` as the query.
- If no query is provided, ask what DITA construct or concept should be looked up.
- Return a grounded summary with the most relevant details first.
