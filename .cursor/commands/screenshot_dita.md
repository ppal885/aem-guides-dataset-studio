# Generate DITA From Screenshot

Use the `aem-guides-dataset-studio` MCP server and call the `generate_dita_from_screenshot` tool.

- Treat everything after `/screenshot_dita` as the image path.
- If no image path is provided, ask for it.
- Present the generation result clearly and mention any review-required blockers.
