# Fix DITA XML

Use the `aem-guides-dataset-studio` MCP server and call the `fix_dita_xml` tool.

- Treat everything after `/fix_dita` as the XML input.
- If no XML is provided, ask the user to paste the DITA XML to fix.
- Show the repaired result and explain the important fixes.
