# Search Jira

Use the `aem-guides-dataset-studio` MCP server and call the `search_jira_issues` tool.

- Treat everything after `/jira` as the search text or key.
- If no argument is provided, ask for a Jira key or search query.
- Present the best matches cleanly instead of raw JSON.
