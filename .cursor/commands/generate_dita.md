# Generate DITA

Use the `aem-guides-dataset-studio` MCP server and call the `generate_dita` tool.

- Treat everything after `/generate_dita` as the `text` argument.
- If the user did not provide a description, ask for it.
- Present the result clearly instead of dumping raw JSON.
