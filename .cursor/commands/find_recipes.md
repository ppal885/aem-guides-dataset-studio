# Find DITA Recipes

Use the `aem-guides-dataset-studio` MCP server and call the `find_recipes` tool.

- Treat everything after `/find_recipes` as the query.
- If no query is provided, ask what recipe or pattern the user wants.
- Return the most relevant recipes with a short explanation of fit.
