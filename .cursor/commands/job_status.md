# Job Status

Use the `aem-guides-dataset-studio` MCP server and call the `get_job_status` tool.

- Treat everything after `/job_status` as the `job_id`.
- If no job ID is provided, ask for it.
- Summarize current status and any next useful action.
