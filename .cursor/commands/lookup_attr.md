# Lookup DITA Attribute

Use the `aem-guides-dataset-studio` MCP server and call the `lookup_dita_attribute` tool.

- Treat everything after `/lookup_attr` as the attribute name.
- If no attribute name is provided, ask for one.
- Summarize meaning, usage, and any verified constraints clearly.
