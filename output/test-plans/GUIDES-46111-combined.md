**Understanding From Jira**

- Issue understood: When several GUID-named DITA files are uploaded together in one drag-and-drop batch and each file independently maps to a different existing asset by fmUuid, AEM Guides is reported to cross-apply one file's UUID-conflict resolution to the others; uploading the same files one at a time is reported to work. Critically, this defect is NOT REPRODUCED by QA: Ajay Sherawat on 2026-08-26 stated "we have been unable to reproduce it in our environment", and a 2026-09-01 re-test on the customer's own Dev server (cm-p125061-e1229364) following the steps showed "The assets were updated as expected after the overwrite".
- Why it matters: Customer context resolved from Jira: Ciena (case E-002177687), Guides 2026.4.0.347, with content integrity at stated risk. Two corrupted end-states are reported - on Cloud Service one uploaded file's content is said to replace content in all existing repository locations; on SDK one file is said to be retained in the upload folder renamed to an existing asset name - but the Crosshair investigation is Paused - Needs Data (label crosshair-needs-clarification) and the root cause is not confirmed, only a working hypothesis (three candidate hypotheses, none confirmed from static analysis, the server-side per-file iteration "appears correctly isolated").
- Requested outcome: In a batch containing multiple independent UUID conflicts, each uploaded file must resolve against its own matching existing asset with no cross-over of binary, rename, move, or overwrite between items - consistent with single-file behaviour; but because reproduction is unconfirmed, the primary deliverable is a reproduction strategy, not fix-validation sign-off.
- Lifecycle understood as: Pre-Development, reproduction-blocked. The Jira is Open, RESOLUTION None, fixVersions Backlog, with no linked fix and no accepted UAC. Because the defect is not reproduced and the root cause is not confirmed, every behaviour-fix criterion is Proposed and gated on reproduction; the prior AI-drafted acceptance criteria on this ticket (10 criteria, 2026-08-19, Needs_Human_Review) asserted a reproducible defect and are superseded by this reproduction-first plan.
- Evidence boundary: Evidence mode: degraded. Live Jira was fetched via JiraClient; both attachments were downloaded and inspected (GUID-files-to-upload.zip = three GUID-named DITA files with distinct fmUuids; case-E-002177687-backup-1.0.zip = a content package restoring existing c_security_overview.dita assets under /content/dam/en/converged_packet_optical). No git clone was synchronized this pass, so git code evidence is unavailable and the implicated Starling paths are provisional and unverified, cited from Crosshair's recorded investigation trace rather than fresh inspection. Product RAG (ask_dita_expert) was not reached and the indexed jira_history tool (search_jira_history) was unavailable this pass, so grounding used the ticket, the attachments, and the recorded Crosshair trace, while the offline jira_qa corpus supplied advisory-only historical candidates that are unvalidated. No fix exists, so the implicated code is current implementation, not changed code.

**Acceptance Criteria**

- AC-01 [Proposed]: (Integration) Given the exact ticket fixtures (case-E-002177687-backup-1.0.zip restored to /content/dam/en/converged_packet_optical and the three GUID-named DITA upload files with their distinct fmUuids) | When QA follows the reproduction approach in Test Scenarios below to force a single-batch multi-UUID-conflict upload with Overwrite on both Cloud Service (cm-p125061-e1229364) and SDK 2026.4.x | Then QA records whether the cross-over defect reproduces, on which build and code path, and under which trigger conditions, and this reproduction result gates every AC below | Evidence: Jira steps to reproduce and non-reproduction comments GUIDES-46111.
- AC-02 [Proposed]: (Basic) Given a batch upload where files A, B, C each independently hit a UUID conflict against different existing assets and the defect has been reproduced | When the batch completes with the default Overwrite action | Then each existing asset receives its own matching uploaded binary and no asset receives another file's binary | Evidence: Jira expected result GUIDES-46111.
- AC-03 [Proposed]: (Negative) Given the same multi-conflict batch on Cloud Service once the defect is reproduced | When the batch completes | Then no single uploaded file's content is written into more than one existing repository location, so the reported Cloud Service symptom must not occur | Evidence: Jira actual result Cloud Service GUIDES-46111.
- AC-04 [Proposed]: (Negative) Given the same multi-conflict batch on SDK once the defect is reproduced | When the batch completes | Then no uploaded file is left in the target upload folder renamed to an existing asset's name, so the reported SDK symptom must not occur | Evidence: Jira actual result SDK GUIDES-46111.
- AC-05 [Proposed]: (Basic) Given the identical files uploaded one at a time | When each single-file upload completes with Overwrite | Then each resolves correctly against its own existing asset, establishing the single-file baseline the batch path must match | Evidence: Jira note that individual uploads do not hit the issue GUIDES-46111.
- AC-06 [Proposed]: (Integration) Given com.adobe.fmdita.config.ConfigManager move-old-file-with-same-UUID (uuid.duplicate.move.old) at its OOTB value true and at false once the defect is reproduced | When the multi-conflict batch is uploaded | Then per-file resolution stays isolated under both config values and the move-old behaviour applies to the correct per-file existing asset | Evidence: Jira configuration reference GUIDES-46111; Experience League upload-dita-content https://experienceleague.adobe.com/en/docs/experience-manager-guides/using/install-conf-guide/migrate-content-cs/upload-dita-content.
- AC-07 [Proposed]: (Integration) Given the build determines which batch code path runs, the legacy ImportServlet flow or the newer AssetServiceImpl v2 batch flow, once the defect is reproduced | When the multi-conflict batch is uploaded on a build using each path | Then per-file UUID-conflict isolation holds on both code paths after QA first confirms which path Guides 2026.4.0.347 invokes | Evidence: Crosshair investigation trace GUIDES-46111 for ImportServlet.java and AssetServiceImpl.java per-file iteration.
- AC-08 [Proposed]: (Integration) Given the batch is uploaded through drag-and-drop as in the steps to reproduce and through the upload button, Files view, and upload API once the defect is reproduced | When each multi-conflict batch completes | Then per-file isolation is consistent across every upload entry point, not only drag-and-drop | Evidence: Jira steps to reproduce single drag-and-drop batch GUIDES-46111.
- AC-09 [Proposed]: (Integration) Given native AEM Assets duplicate detection is enabled once the defect is reproduced | When the multi-conflict batch is uploaded | Then the Guides batch-conflict flow neither suppresses nor cross-triggers native checksum duplicate detection, a separate mechanism from fmUuid conflict resolution | Evidence: Jira clarification that the issue is not about generic dam:sha1 duplicate detection GUIDES-46111; Experience League detect-duplicate-assets https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/assets/admin/detect-duplicate-assets.
- AC-10 [Proposed]: (Integration) Given the batch UUID-conflict unit coverage in AssetServiceImplBatchTest currently uses single-item lists only | When a test exercises multiple items each with a distinct existing-UUID conflict in one batch | Then such a test exists and asserts per-file isolated resolution, closing a confirmed coverage gap that holds regardless of whether the field defect reproduces | Evidence: Crosshair finding GUIDES-46111 at AssetServiceImplBatchTest line 263 single-item list.

**Expected Behaviour**

- Per the ticket's expected result, batch conflict resolution must be isolated per file: file A resolves against existing asset A, file B against B, file C against C, with no cross-over of binary, rename, move, or overwrite.
- Single-file and multi-file upload must be logically consistent; single-file is the known-good baseline.
- UUID (fmUuid) conflict resolution is explicitly distinct from friendly-name/title collisions and from native dam:sha1 duplicate detection; the plan keeps these separate.
- The root cause is not confirmed. Crosshair's static analysis found the server-side per-file iteration "appears correctly isolated" and could not distinguish among three candidate hypotheses (xmleditor InputStream reuse, session/workspace visibility, unique-name collision); the correct oracle for AC-02 to AC-09 depends on which hypothesis reproduction confirms.

**Scope From Git**

- Lifecycle stage: Pre-Development, reproduction-blocked; no development branch, PR, commit, or fix version is linked to GUIDES-46111 (fixVersions Backlog).
- No product or automation clone was synchronized this pass; the implicated code is cited from Crosshair's recorded investigation trace, not from a fresh clone inspection, so no captured SHA is claimed for it and the paths are provisional pending direct inspection.

**Code Touched**

- No code changes yet - development has not started, and no root cause is confirmed.
- Current implementation implicated (from Crosshair trace, provisional): Starling core/utils/src/main/java/com/adobe/fmdita/servlets/ImportServlet.java (legacy drag-drop batch flow; handleOtherImportAssetOperations per-file iteration, UUIDExistsException throw site, OVERWRITE_OP resolution).
- Current implementation implicated (from Crosshair trace, provisional): Starling core/assets/src/main/java/com/adobe/guides/assets/core/service/AssetServiceImpl.java importSingleAsset and resolveImportUuidConflict (v2 batch flow; workspace move on duplicate UUID with moveOldOnDuplicateUuid=true, forceGuid=true).
- Current implementation boundary: Crosshair could not distinguish which hypothesis holds from source alone; xmleditor InputStream handling and low-level session or workspace visibility are not proven, so no code claim is asserted as the cause before reproduction and runtime evidence.

**Lines Changed**

- Not applicable - development has not started.

**Test Scenarios**

- Reproduction approach (primary deliverable while reproduction is unconfirmed): the goal is to reliably reproduce or definitively rule out the multi-UUID-conflict cross-over before any fix AC is validated, since QA's 2026-09-01 attempt did not reproduce it. Restore case-E-002177687-backup-1.0.zip to /content/dam/en/converged_packet_optical so the three existing c_security_overview.dita assets exist at distinct paths with distinct fmUuids, then upload the three GUID-named DITA files from GUID-files-to-upload.zip; note the description names GUID-b6f857bf-797b-4a45-b3f7-53697cbed666-en.dita but the real attached fixture is GUID-b6f857b4-797b-4a45-b3f7-53697cbed666-en.dita, and exact fmUuid matching is the trigger.
- Trigger conditions to vary systematically, because the working hypothesis is a batch and timing race against a shared JCR session: (a) environment - Cloud Service cm-p125061-e1229364 versus SDK 2026.4.x, which show different end-states; (b) build and code path - confirm whether 2026.4.0.347 invokes the legacy ImportServlet flow or the AssetServiceImpl v2 batch flow first; (c) batch size - two versus three or more simultaneous conflicts; (d) target folder exactly /content/dam/en/converged_packet_optical; (e) the default Overwrite action; (f) upload timing and concurrency - force the files to enter the UUID-conflict path as close to simultaneously as possible. Capture the multipart POST payload or HAR, the overwrite log lines, and a per-asset byte-compare each attempt. Disposition: once reproduced, record the confirming trigger set and code path and validate AC-02 to AC-09 against the confirmed oracle; if it cannot be reproduced across these conditions, recommend triage as needs-clarification rather than asserting a fix, and close on the confirmed unit-coverage gap AC-10.
- Test data to prepare: the two attached ZIPs restored/extracted exactly as above; access to Cloud Service Dev Author cm-p125061-e1229364 and an SDK 2026.4.x instance; the /content/dam/en/converged_packet_optical target; ConfigManager access to toggle uuid.duplicate.move.old; HAR capture and Splunk/log access.
- P0 [TS-01] [AC-01]: Action: execute the reproduction strategy end to end on Cloud Service and SDK, varying environment, batch size, code path, and timing, capturing HAR, logs, and per-asset byte-compares. Expected: a documented reproduction verdict per trigger set - reproduced (with the confirming conditions and code path) or not reproduced across all varied conditions.
- P0 [TS-02] [AC-05]: Action: upload files A, B, C one at a time with Overwrite. Expected: each resolves against its own existing asset correctly, establishing the single-file baseline.
- P1 [TS-03] [AC-02, AC-03] (gated on TS-01 reproduction): Action: on Cloud Service, upload the three files as one drag-and-drop batch with Overwrite. Expected: each existing asset gets its own matching binary; no file's content appears in more than one location.
- P1 [TS-04] [AC-04] (gated on TS-01 reproduction): Action: on SDK, upload the same batch with Overwrite. Expected: no uploaded file is left renamed in the upload folder and per-file resolution is correct.
- P1 [TS-05] [AC-06] (gated on TS-01 reproduction): Action: repeat the batch with uuid.duplicate.move.old at true and false. Expected: per-file isolation holds and move-old applies to the correct per-file existing asset under both values.
- P1 [TS-06] [AC-07] (gated on TS-01 reproduction): Action: confirm which batch code path the build uses, then run the batch on a build for each path. Expected: per-file isolation holds on both the legacy and v2 batch flows.
- P1 [TS-07] [AC-08] (gated on TS-01 reproduction): Action: run the multi-conflict batch via drag-and-drop, upload button, Files view, and upload API. Expected: consistent per-file isolation across all entry points.
- P1 [TS-08] [AC-09] (gated on TS-01 reproduction): Action: with native duplicate detection enabled, run the multi-conflict batch. Expected: native dam:sha1 duplicate detection behaves independently and is neither suppressed nor cross-triggered by the fmUuid conflict flow.
- P0 [TS-09] [AC-10]: Action: add/execute a unit test in AssetServiceImplBatchTest that submits multiple items each with a distinct existing-UUID conflict in one batch. Expected: the test exists and asserts isolated per-file resolution; today no such test exists (single-item lists only).

**Known Jira Bugs / Past Similar Tickets**

- GUIDES-33693 "DITA Content Not Updating with Latest Image When Re-Uploaded via Guides UI" - Similarity: strongest match, same asset-identity-on-upload class where a re-upload does not reflect the new binary; Status: to be validated live; Resolution: to be validated live; Affected version: not confirmed offline; Fix version: not confirmed offline; RCA: not confirmed offline; Test evidence: none retrieved offline; Impact: same content-integrity-on-upload risk, a strong same-mechanism neighbour to mine for Negative and Integration scenarios.
- GUIDES-37836 "[SLA3] Copying folders via Assets UI duplicates fmUuid" - Similarity: adjacent match, fmUuid identity mishandled on a bulk asset operation; Status: to be validated live; Resolution: to be validated live; Affected version: not confirmed offline; Fix version: not confirmed offline; RCA: not confirmed offline; Test evidence: none retrieved offline; Impact: the folder-copy duplicate-GUID class the ticket itself cites as related, a candidate Regression Area.
- GUIDES-33739 "[SLA3] Source Assets Deleted After Copy & Reprocess in AEM Assets when Guides" - Similarity: adjacent match, copy and post-processing path mishandling asset identity; Status: to be validated live; Resolution: to be validated live; Affected version: not confirmed offline; Fix version: not confirmed offline; RCA: not confirmed offline; Test evidence: none retrieved offline; Impact: related bulk-operation identity class, a candidate Regression Area.
- GUIDES-4251 "Config to control behavior of GUID file import via WebDAV" - Similarity: weak setup-only match, GUID-file import configuration precedent; Status: to be validated live; Resolution: to be validated live; Affected version: not confirmed offline; Fix version: not confirmed offline; RCA: not confirmed offline; Test evidence: none retrieved offline; Impact: configuration precedent relevant to the uuid.duplicate.move.old branch.
- Search status: surfaced from the offline jira_qa corpus (embed_query and query_collection over the indexed history) because the live search_jira_history MCP was not reachable this pass. Narrow JQL intents run offline were an error-symptom search for a re-upload not updating the latest binary on overwrite, a workflow search for UUID duplicate move-old conflict handling in the upload post-processing path, and a configuration-key search for folder-copy fmUuid duplication; all candidates are offline-only, so validate mutable Jira status, resolution, and fix version live before relying on them.

**Regression Areas**

- Re-test single-file GUID-named upload with Overwrite, the known-good baseline the batch path must match.
- Re-test batch upload where files do NOT conflict, to confirm the fix does not regress the normal multi-file path.
- Re-test the move-old-on-duplicate-UUID behaviour (uuid.duplicate.move.old) for a single conflict, since the fix touches per-file conflict resolution.
- Re-test folder copy / copy-reprocess fmUuid handling (GUIDES-37836, GUIDES-33739 class) because they share fmUuid identity logic on bulk operations.
- Re-test native AEM Assets duplicate detection (dam:sha1) on batch upload because the Guides conflict flow shares the upload path and must not perturb it.
- Re-test both the legacy ImportServlet batch flow and the AssetServiceImpl v2 batch flow if the build/code path differs across supported releases.

**Automation Coverage & Gaps**

- Main feature coverage: Unverified - the field defect is not reproduced, so no end-to-end automation can assert the fixed behaviour yet.
- AC-01 - Not covered: at the integration layer, setup the exact ticket fixtures and a scripted multi-UUID-conflict batch upload, poll repository state with the upload timeout, assert the recorded reproduction verdict per trigger set, cleanup the fixtures and uploaded assets, and tag for Assets and Cloud runs.
- AC-02 - Not covered: at the integration layer, setup a three-item batch each hitting a distinct existing-UUID conflict, poll each asset with the upload timeout, assert each existing asset receives only its own matching binary, cleanup all assets, and tag for Assets and Cloud runs.
- AC-03 - Not covered: at the integration layer, setup the multi-conflict batch on Cloud Service, poll all existing locations with the upload timeout, assert no file's content appears in more than one location, cleanup all assets, and tag for Assets and Cloud runs.
- AC-04 - Not covered: at the integration layer, setup the multi-conflict batch on SDK, poll the target folder with the upload timeout, assert no upload is left renamed to an existing asset name, cleanup all assets, and tag for Assets and Cloud runs.
- AC-05 - Not covered: at the integration layer, setup three single-file uploads each with a distinct UUID conflict, poll each with the upload timeout, assert correct per-file resolution as the baseline, cleanup all assets, and tag for Assets and Cloud runs.
- AC-06 - Not covered: at the integration layer, setup the multi-conflict batch with uuid.duplicate.move.old at true and false, poll each result with the upload timeout, assert isolated per-file resolution under both values, cleanup all assets, and tag for Assets and Cloud runs.
- AC-07 - Not covered: at the integration layer, setup the multi-conflict batch on a build for the legacy and the v2 batch code path, poll each with the upload timeout, assert per-file isolation on both, cleanup all assets, and tag for Assets and Cloud runs.
- AC-08 - Not covered: at the UI layer, setup the multi-conflict batch through drag-and-drop, the upload button, Files view, and the upload API, poll each with the upload timeout, assert consistent per-file isolation across entry points, cleanup all assets, and tag for Assets and Cloud runs.
- AC-09 - Not covered: at the integration layer, setup the multi-conflict batch with native duplicate detection enabled, poll the detection result with the upload timeout, assert native detection is neither suppressed nor cross-triggered, cleanup all assets, and tag for Assets and Cloud runs.
- AC-10 - Not covered: at the unit layer in AssetServiceImplBatchTest, setup a batch of multiple items each with a distinct existing-UUID conflict rather than a single-item list, poll the in-memory result with the test timeout, assert isolated per-file resolution, cleanup the test fixtures, and tag for the batch unit suite; this is the one gap closable now.

**Open Questions**

- OQ-01: Which batch code path does Guides 2026.4.0.347 actually invoke for a drag-and-drop upload - the legacy ImportServlet flow or the AssetServiceImpl v2 flow (AssetServletV2 change)? QA impact: determines which code path and oracle the reproduction and AC-07 target.
- OQ-02: What exact trigger conditions (environment, batch size, timing/concurrency, build) are required to reproduce the cross-over, given QA could not reproduce it on the customer Dev server on 2026-09-01? QA impact: without a deterministic trigger, AC-02 to AC-09 cannot be validated and the ticket may be triaged as needs-clarification.
- OQ-03: Is the reported behaviour a genuine per-file isolation defect, or an artifact of the specific fixtures/timing in the original report? QA impact: decides whether this is a fix ticket or should be closed on the confirmed unit-coverage gap (AC-10) with a documented non-reproduction.
