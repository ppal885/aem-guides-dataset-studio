**Understanding From Jira**
- Issue understood: A configured friendly name for a DITA attribute is not shown consistently in Full Tags View, the Condition Attributes panel, and the Right Panel.
- Why it matters: Customer context resolved from Jira: TxDOT from the Jira label; authors still see technical raw names even after an administrator provides clearer labels.
- Requested outcome: Every in-scope editor surface shows the effective friendly name, keeps a safe raw-name fallback, and respects the active Editor profile configuration.
- Lifecycle understood as: Implementation Review because xmleditor PR 8069 exists and its cached branch diff was inspected against cached origin/develop.
- Evidence boundary: Evidence mode: full; backend JiraClient returned the six current AI-generated Acceptance Criteria, five comments, and the attachment. Four ask_dita_expert probes, two indexed Jira-history searches, cached product refs, current configuration code, and automation code were also inspected. Figma is not applicable. Git refs were not fetched, and the supplied new-editor product path has no product repository. All criteria remain Proposed because Jira is marked Needs_Human_Review.

**Acceptance Criteria**
- AC-01 [Proposed]: (Basic) Given an attribute has a friendly name in the active Editor configuration | When an author opens Full Tags View for an element that uses the attribute | Then Full Tags View shows the saved friendly name | Evidence: Jira description and attachment GUIDES-49507.
- AC-02 [Proposed]: (Basic) Given a conditional attribute has a friendly name in the active Editor configuration | When an author opens the Condition Attributes panel | Then the panel shows the saved friendly name | Evidence: Jira description, attachment, and commit 839d95b9 source file src/model/condition.ts L50-L53 for GUIDES-49507.
- AC-03 [Proposed]: (Basic) Given an attribute has a friendly name and is valid for the selected element | When an author applies it from the Right Panel | Then the Right Panel keeps the saved friendly name in the selection and applied row | Evidence: Jira description, attachment, and commit 839d95b9 source file src/model/other_attrs.ts L348-L358 for GUIDES-49507.
- AC-04 [Proposed]: (Negative) Given a valid custom or built-in attribute has no friendly name | When an author views it in Full Tags View, Condition Attributes, or the Right Panel | Then the surface shows its defined nonblank fallback: raw name for custom attributes, default label for built-in conditional attributes | Evidence: Jira AC-04 GUIDES-49507 and commit 839d95b9 source files src/model/attribute.ts L9-L15 and src/model/condition.ts L50-L53.
- AC-05 [Proposed]: (Integration) Given a mapped attribute is visible in Full Tags View, Condition Attributes, and the Right Panel | When an administrator adds, changes, or removes its friendly name | Then rendered rows plus the open dropdown update before reload or panel reopen, and removal restores the AC-04 fallback | Evidence: Jira AC-05 GUIDES-49507 and commit 839d95b9 source file src/controllers/editor/xml_markup_author_controller.ts L905-L929.
- AC-06 [Proposed]: (Integration) Given a valid custom conditional attribute belongs to the active profile and schema | When its name is added to the supported conditional-attribute list | Then the editor discovers it at runtime and shows its mapped or fallback label without a product-code change | Evidence: Jira AC-06 GUIDES-49507 and C:/starling/core/utils/src/main/java/com/adobe/fmdita/service/FetchCondAttrListService.java L27-L58.
- AC-07 [Proposed]: (Integration) Given two signed-in authors open topics governed by different active profiles | When each author views the same raw attribute | Then each session shows the label from its topic's active profile without using the other profile's mapping | Evidence: Jira reviewer comment and C:/xmleditor/xmleditor/src/controllers/app_controller.ts L466-L471 for GUIDES-49507.
- AC-08 [Proposed]: (Integration) Given Use only above attributes is enabled with configured valid choices | When an author opens Add Attribute for the selected element | Then the list shows only choices that are both configured and valid for that element | Evidence: Jira reviewer comment and C:/xmleditor/xmleditor/src/model/other_attrs.ts L131-L143 for GUIDES-49507.
- AC-09 [Proposed]: (Integration) Given a topic already contains an attribute excluded by Use only above attributes | When an author reopens the topic with that setting enabled | Then the existing attribute remains visible in the topic and present in its source XML | Evidence: Jira reviewer comment and C:/xmleditor/xmleditor/src/model/other_attrs.ts L131-L143 for GUIDES-49507.
- AC-10 [Proposed]: (Integration) Given a profile has saved friendly-name mappings before a supported AEM Guides upgrade | When an author opens the same topics after the upgrade | Then the same labels and active-profile scope still apply | Evidence: Jira reviewer comment GUIDES-49507 and the inspected guides-ui-tests upgrade suite.
- AC-11 [Proposed]: (Negative) Given element friendly names already work in Full Tags View and Quick Insert | When an administrator saves an attribute friendly-name change | Then the existing element friendly names remain unchanged | Evidence: Jira reviewer comment and commit 839d95b9 source file src/controllers/editor/xml_markup_author_controller.ts L919-L928 for GUIDES-49507.

**Expected Behaviour**
- Jira-verified behavior: the same configured attribute label is requested in Full Tags View, the Condition Attributes panel, and the Right Panel during selection and after application.
- Attachment-verified behavior: the saved mapping product to category is ignored in all three shown surfaces; each surface still shows Product or product.
- Code-verified behavior from cached PR ref 839d95b9: Attribute stores a friendly display value with raw fallback, the applied Right Panel row uses friendly lookup with fallback, and Condition group paths use friendly lookup with fallback.
- Code-verified risk from cached PR ref 839d95b9: the live Tag View update calls a guarded cross-package command, so a build without that command silently skips the attribute update.
- Code-verified risk from cached PR ref 839d95b9: conditional setup seeds eight fixed attribute names before adding runtime values; a new configured name is not proven to become a rendered attribute.
- Code-verified profile rule: attribute mappings and the restriction flag are Editor configuration values saved for the active folder profile, with the global profile used as the fallback.
- Code-verified restriction rule: Use only above attributes filters available choices but retains an attribute that is already applied to the element.
- Code-verified conditional-file rule: Starling reads the apps overlay before the libs default and checks legacy XML before CSV. Repeatable tests must not edit product-owned libs content.
- RAG accepted finding: AEM Guides documents general XML Editor configuration at global-profile and folder-profile levels, but the retrieved documentation does not prove that friendly attribute labels are stored per user.
- RAG partial finding: Condition Presets are documented as an attribute-selection surface, but friendly-name display is not documented there. Full Tags View, Right Panel, DITAVAL, and Preview friendly-name behavior was not established by RAG.
- RAG partial finding: documentation identifies the shipped conditional-attribute list and version-dependent legacy XML, while inspected Starling code supplies the exact precedence rule.
- RAG rejected finding: the retrieved explanation of Use only above attributes relied on analogy with the element setting, so inspected code and runtime testing remain the authority for AC-08 and AC-09.
- Unknown from current evidence: conditional value labels, Condition Presets, the DITAVAL attribute dropdown, Preview, and the shipped cross-package command version remain Open Questions.

**Scope From Git**
- Lifecycle stage: Implementation Review; live Jira GUIDES-49507 and Jira comment PR 8069 are the issue and implementation sources.
- Product clone: `C:\xmleditor\xmleditor`; branch develop; worktree SHA 1f8e3996e40ea4c55cea2e11c52d7ddb6f58b09a; upstream origin/develop; cached ahead 0 and behind 1777; fetch not run; dirty with 8 entries. The inspected cached refs are origin/develop at SHA 85574ec9f6a25ea82ef97f19b83b2f66e0cd90bb and origin/crosshair/guides-49507 at SHA 839d95b938e70cf4e5e4a7e51ab7a8d32460df86.
- Product clone: `C:\starling`; branch develop; SHA fdfa72777a2d73b2cdba6d2bdd60ea5535bad75f; upstream origin/develop; cached ahead 0 and behind 1230; fetch not run; dirty with 13 entries. It was used only for current conditional-file precedence evidence.
- Product path: `C:\ui_framework\new_editor`; no product Git repository was found, so new-editor implementation claims remain unverified.
- Automation clone: `C:\ui_framework\new_editor\guides-ui-tests`; branch develop; SHA bf5dca679ec84f6128d4fe8d9b3b12201e2cc5bb; upstream origin/develop; cached ahead 0 and behind 1000; fetch not run; dirty with 2 entries.
- PR discovery: Jira comment identifies xmleditor PR 8069; the local cached remote branch with the same GUIDES-49507 change was inspected without switching branches or changing the dirty worktree.
- Figma and design evidence: not applicable because this ticket corrects labels in existing editor surfaces and supplies no design link.

**Code Touched**
- Changed in cached ref 839d95b9: `src/model/attribute.ts` L3-L15 adds the display value; `src/config/ui/views/widgets/other_attribute_list_item.json` L17-L18 renders it; `src/model/other_attrs.ts` L348-L358 uses it in an applied row.
- Changed in cached ref 839d95b9: `src/model/condition.ts` L50-L53 resolves the Condition label; `src/controllers/editor/xml_markup_author_controller.ts` L443-L444 and L905-L929 route friendly-name updates; L1535-L1564 initializes conditional attributes.
- Tests changed in cached ref 839d95b9: `src/controllers/editor/__tests__/xml_markup_author.initialization.test.ts`, `src/model/condition.test.ts`, and `src/model/other_attrs.test.ts` cover command forwarding and model-level friendly-name fallback.
- Current configuration implicated: `C:\xmleditor\xmleditor\src\config\ui\views\widgets\xml_editor_attributes_settings.json` L5-L8 binds attribute mappings and the constraint to Editor configuration.
- Current profile persistence implicated: `C:\xmleditor\xmleditor\src\controllers\widgets\dita_item_list.ts` L53-L56 and L119-L146 serializes mappings and the constraint for the active profile.
- Current conditional configuration implicated: `C:\starling\core\utils\src\main\java\com\adobe\fmdita\service\FetchCondAttrListService.java` L27-L58 defines XML and CSV lookup plus apps-over-libs precedence.
- Adjacent surfaces not changed by the PR: `src/controllers/widgets/condition_preset_editor_controller.ts` L574-L575 and `src/controllers/widgets/ditaval_element_controller.ts` L22-L33 still construct raw attribute labels.

**Lines Changed**
- Cached origin/develop to origin/crosshair/guides-49507 diff: 8 files changed, 154 insertions, and 20 deletions.
- File summary: six existing TypeScript or JSON files are modified, one Condition test file is added, and two existing test files are extended.

**Test Scenarios**
- Test data to prepare: use a supported build containing PR 8069; a standard product attribute mapped to mixed-case label TxDOT Product Class; a valid custom DTD attribute customflag; a custom conditional attribute market-segment added through an apps overlay with values; topics inside and outside a folder-profile scope; a global fallback mapping; Use only above attributes on and off; a topic with mapped, unmapped, already-applied, and element-friendly-name examples; a pre-upgrade configuration snapshot; exact label text, raw XML, active-profile, and source-view oracles; and cleanup that restores the profiles, overlay, and topics.
- P0 [TS-01] [AC-01]: Action: open Full Tags View for standard and custom attributes with saved mixed-case labels. Expected: each tag shows the exact saved label and the raw XML attribute name is unchanged.
- P0 [TS-02] [AC-02]: Action: open the Condition Attributes panel for a mapped built-in conditional attribute. Expected: the group heading shows the exact saved label with its original casing.
- P0 [TS-03] [AC-03]: Action: select, apply, and reopen a mapped attribute in the Right Panel. Expected: the exact saved label appears in the dropdown, edit state, and applied row.
- P0 [TS-04] [AC-04]: Action: remove mappings from customflag and one built-in conditional attribute, then inspect all three affected surfaces. Expected: customflag shows its raw name, the built-in shows its default label, and neither label is blank.
- P0 [TS-05] [AC-05]: Action: add, change, and remove one mapping while its rows and dropdown stay open. Expected: Full Tags View, Condition Attributes, and the Right Panel update without reload or panel reopen; removal restores the AC-04 fallback.
- P0 [TS-06] [AC-06]: Action: add market-segment through the supported apps overlay for the active profile and schema, then open a valid element. Expected: the editor discovers it without a product-code change and shows its mapped label or AC-04 fallback.
- P1 [TS-07] [AC-07]: Action: have two authors open the same raw attribute in topics governed by different active profiles. Expected: each session shows only its topic's applicable label and does not leak the other mapping.
- P1 [TS-08] [AC-08]: Action: enable Use only above attributes and inspect two elements with different DTD-valid choices. Expected: each Add Attribute list contains only choices allowed by both the profile and the selected element.
- P1 [TS-09] [AC-09]: Action: reopen a topic that already contains an excluded attribute while Use only above attributes is enabled. Expected: the attribute remains visible and unchanged in source XML.
- P1 [TS-10] [AC-10]: Action: capture the mappings before upgrade and repeat TS-01 through TS-03 after upgrade. Expected: the same labels and active-profile scope remain effective after upgrade.
- P1 [TS-11] [AC-11]: Action: save an attribute-label change and reopen Full Tags View plus Quick Insert. Expected: existing element friendly names retain their saved text and behavior.
- Deferred surface checks: add Condition Presets, DITAVAL, Preview, and conditional value-label scenarios only after OQ-01 through OQ-04 receive explicit scope decisions.

**Known Jira Bugs / Past Similar Tickets**
- Observed Customer Jira Profile: TxDOT - unavailable because no approved aggregate customer-profile artifact was inspected; only the explicit current-ticket label is used as customer context.
- Historical search status: search_jira_history ran a TxDOT-scoped Editor error and symptom query plus a cross-customer Editor workflow and rendering-mechanism query, equivalent to two narrow JQL intents. Both searched 22504 indexed jira_qa chunks and returned no qualified same-mechanism match.
- The cross-customer search rejected GUIDES-19327 as area-only overlap; it shares friendly-name words but not a verified root cause, configuration key, execution path, or behavior contract.
- No same-mechanism historical Jira is established from current evidence. This means no match surfaced in the indexed corpus, not that no such ticket exists.

**Regression Areas**
- Recheck raw XML after each label operation and confirm the stored attribute name and value are unchanged, because friendly names are display metadata and must not rewrite authored DITA.
- Recheck all eight built-in conditional attributes with mapped and unmapped labels, because the PR replaces their prior fixed display labels with profile-aware lookup and fallback behavior.
- Recheck folder-profile and global-profile isolation for two author sessions, because a stale cached mapping could expose a label from the wrong content scope.
- Recheck the attribute dropdown, edit row, and applied row separately for an unmapped attribute, because two current lookup paths do not add an explicit raw-name fallback.
- Recheck the apps overlay, libs fallback, and legacy XML precedence without editing libs, because upgrade-safe customer configuration must not depend on modifying shipped content.
- Recheck existing element friendly names in Full Tags View and Quick Insert, because attribute updates now share the controller path used by the established element feature.

**Automation Coverage & Gaps**
- Main feature coverage: Not covered - Layer: UI. Setup: configure mapped and unmapped attributes. Poll: wait for each covered editor surface. Timeout: use existing panel timeouts. Assert: check exact labels, fallback, scope, and persistence. Cleanup: restore profiles and fixtures. Tag: editor-friendly-attribute-names.
- AC-01 - Not covered: Layer: UI. Setup: map one attribute label. Poll: wait for Full Tags View. Timeout: use the existing panel-load timeout. Assert: check the rendered label. Cleanup: restore the mapping. Tag: editor-friendly-attribute-names.
- AC-02 - Not covered: Layer: UI. Setup: map one conditional attribute label. Poll: wait for Condition Attributes. Timeout: use the existing panel-load timeout. Assert: check the rendered group label. Cleanup: restore the mapping. Tag: editor-friendly-attribute-names.
- AC-03 - Not covered: Layer: UI. Setup: map one valid attribute label. Poll: wait for the Right Panel states. Timeout: use the existing panel-load timeout. Assert: check selection, edit, and applied-row labels. Cleanup: restore the mapping. Tag: editor-friendly-attribute-names.
- AC-04 - Not covered: Layer: UI. Setup: create an unmapped custom DTD attribute. Poll: wait for each named panel to render. Timeout: use the existing panel-load timeout. Assert: check raw fallback with no blank label. Cleanup: remove the custom fixture. Tag: editor-friendly-attribute-names.
- AC-05 - Not covered: Layer: UI. Setup: keep a mapped topic open and prepare an admin mapping change. Poll: wait for the open editor to receive configuration state. Timeout: use a short configured UI wait. Assert: check all three labels without reload. Cleanup: restore the mapping. Tag: editor-friendly-attribute-names.
- AC-06 - Not covered: Layer: UI and configuration API. Setup: reuse `C:\ui_framework\new_editor\guides-ui-tests\tests\editor_folder_profile\conditional_attribute_ui_edit.feature` L54-L62 and add an apps-overlay canary. Poll: wait for the saved profile and editor panel. Timeout: use existing profile-save and panel timeouts. Assert: check the canary label on every applicable surface. Cleanup: remove the overlay and profile. Tag: editor-folder-profile.
- AC-07 - Not covered: Layer: UI. Setup: create global and folder mappings plus topics in both scopes. Poll: wait for each topic profile to load. Timeout: use the existing editor-open timeout. Assert: check the expected mapping and no cross-profile leakage. Cleanup: delete the test folder profile. Tag: editor-folder-profile.
- AC-08 - Not covered: Layer: UI. Setup: configure a restricted list and two elements with different valid attributes. Poll: wait for Add Attribute. Timeout: use the existing panel timeout. Assert: check the intersection of configured and element-valid choices. Cleanup: disable the restriction. Tag: editor-folder-profile.
- AC-09 - Not covered: Layer: UI. Setup: prepare a topic with a pre-existing excluded attribute. Poll: reopen the topic with the restriction enabled. Timeout: use the existing editor-open timeout. Assert: check the visible attribute and unchanged source XML. Cleanup: disable the restriction. Tag: editor-folder-profile.
- AC-10 - Not covered: Layer: upgrade UI. Setup: reuse the existing guides-ui-tests upgrade capture to save profile configuration. Poll: wait for the upgraded editor and profile data. Timeout: use the upgrade suite timeout. Assert: compare exact labels before and after upgrade. Cleanup: restore the upgrade fixture. Tag: upgradeonly.
- AC-11 - Not covered: Layer: UI. Setup: configure existing element labels and one attribute mapping. Poll: wait for Full Tags View and Quick Insert after saving. Timeout: use the existing editor panel timeout. Assert: compare element labels with the baseline. Cleanup: restore both mappings. Tag: editor-friendly-names-regression.

**Open Questions**
- OQ-01: Are conditional value labels from the Name, Value, and Label configuration in scope with attribute names? QA impact: a yes adds value-label checks in the Conditions panel and Right Panel; a no keeps this plan limited to attribute names.
- OQ-02: Must Condition Presets show configured attribute friendly names? QA impact: a yes adds the surface to acceptance and UI automation; a no records it as an explicit exclusion.
- OQ-03: Must the DITAVAL attribute dropdown show configured attribute friendly names? QA impact: a yes adds DITAVAL fixtures and dropdown assertions; a no records it as an explicit exclusion.
- OQ-04: Is any attribute label visible in Preview required to use the friendly name? QA impact: a yes requires the exact Preview surface and oracle; a no removes Preview from runtime sign-off.
- OQ-05: Which shipped markup-editor version provides the guarded attribute-update command? QA impact: the answer determines whether AC-05 can pass in the target build or requires a dependency update.
- OQ-06: Which conditional-list source is supported when legacy XML and CSV both exist? QA impact: the answer fixes the upgrade matrix, precedence fixture, and safe cleanup path for AC-06 and AC-10.
