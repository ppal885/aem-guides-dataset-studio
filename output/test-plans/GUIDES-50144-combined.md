**Understanding From Jira**
- Issue understood: A delegated Admin User for a non-global Folder Profile cannot add or remove other Admin Users unless the caller also belongs to the exact local administrators group; the page reports success even though the membership change is not saved.
- Why it matters: Customer context resolved from Jira: Zebra Technologies uses delegated Folder Profile administration to govern snippets, conditional attributes, templates, and output presets without granting broad AEM administrator access, so this regression blocks its approved governance model.
- Requested outcome: Restore add and remove access for the target Folder Profile's delegated Admin Users, keep the local-administrator and global-profile boundaries unchanged, and return a real authorization failure to callers who have neither allowed role.
- Lifecycle understood as: Implementation Review for Starling PR #8091 at merged commit eac2c72512306061d2110b02e03d28197eadeecf; the Jira is in Stage, but the exact validation build is still an Open Question.
- Evidence boundary: Evidence mode: full; live Jira and comments, all three downloaded Jira attachments, four ask_dita_expert probes, two indexed Jira-history searches, the disabled evidence-graph response, PR history, and four local product or automation clones were inspected. Both recordings in the attachment archive were extracted and compared. The clones were not fetched or changed during this task, so current-remote claims are restricted to the captured SHAs and local refs.

**Acceptance Criteria**
- AC-01 [Proposed]: (Basic) Given a delegated folder-profile admin without local administrator membership | When the admin adds another Admin User to the same non-global profile | Then the added user remains in Admin Users after a page refresh | Evidence: Jira UAC GUIDES-50144 and commit eac2c72512306061d2110b02e03d28197eadeecf.
- AC-02 [Proposed]: (Basic) Given a delegated folder-profile admin without local administrator membership | When the admin removes an Admin User from the same non-global profile | Then the removed user stays absent after a page refresh | Evidence: Jira UAC GUIDES-50144 and commit eac2c72512306061d2110b02e03d28197eadeecf.
- AC-03 [Proposed]: (Basic) Given a member of the exact local administrators group uses a non-global folder profile | When the administrator adds or removes one Admin User | Then the requested membership change remains saved after refresh | Evidence: Jira UAC GUIDES-50144 and C:\starling\core\utils\src\main\java\com\adobe\fmdita\common\ACLUtils.java L314-L327.
- AC-04 [Proposed]: (Negative) Given a delegated folder-profile admin opens the global folder profile | When the admin submits an Admin Users change | Then the global Admin Users list stays unchanged | Evidence: Jira UAC GUIDES-50144 and C:\starling\AEM6.x\repo\jcr_root\libs\fmdita\clientlibs\folderprofiles\js\main.js L788-L797.
- AC-05 [Proposed]: (Negative) Given an unauthorized user has neither target-profile admin membership nor local administrator membership | When the user submits an Admin Users change | Then the client reports an authorization error without changing stored membership | Evidence: Jira UAC GUIDES-50144 and C:\starling\core\publish-listener\src\main\java\com\adobe\fmdita\rest\folderprofiles\FolderProfilesAPI.java L639-L650.

**Expected Behaviour**
- Jira defines two allowed roles for a non-global profile: membership in that profile's own Admin User group, or membership in AEM's exact local administrators group. Other AEM service or product groups do not replace either role.
- Jira lists authors, Contributors, AEM Administrators author Program 167205 Environment 1910614, folderprofile-8fbxxxx, everyone, AEM Assets Collaborator Users - Service, AEM Sites Content Managers - Service, AEM Administrators - Service, and Analytics Administrators. Only the target folderprofile-<id> group or exact local administrators group satisfies this authorization check.
- The inspected authorization helpers support that split: `ACLUtils.isAdmin` checks the repository admin identity or exact administrators-group membership at `C:\starling\core\utils\src\main\java\com\adobe\fmdita\common\ACLUtils.java` L314-L327, while `ACLUtils.isGroupMember` checks membership in the target profile's group at L342-L356.
- The profile service creates `folderprofile-<profile-id>` by default and adds the supplied Admin Users at `C:\starling\core\utils\src\main\java\com\adobe\fmdita\folderprofiles\FolderProfileService.java` L1083-L1098. Later updates add and remove declared group members at L1121-L1171.
- The pre-fix worktree snapshot gates Admin User writes on local-administrator access only at `C:\starling\core\publish-listener\src\main\java\com\adobe\fmdita\rest\folderprofiles\FolderProfilesAPI.java` L625-L646. Commit eac2c72512306061d2110b02e03d28197eadeecf widens this non-global-profile gate to local administrator or target Folder Profile admin while preserving the global-profile guard.
- AC-05 is not implemented by PR #8091. The handler still reaches `saveProfileChanges` for an unauthorized caller at `C:\starling\core\publish-listener\src\main\java\com\adobe\fmdita\rest\folderprofiles\FolderProfilesAPI.java` L649-L650, and that method returns HTTP 200 with true at L853-L862. The UI treats that response as “Profile Saved” at `C:\starling\AEM6.x\repo\jcr_root\libs\fmdita\clientlibs\folderprofiles\js\main.js` L233-L249.
- Product RAG was accepted only for the documented Admin Users surface and the narrower Folder Profile administration scope. RAG did not prove the exact group-provisioning or authorization rules, so those conclusions use Jira and inspected code instead.

**Scope From Git**
- Lifecycle stage is Implementation Review; review scope is the merged Starling commit eac2c72512306061d2110b02e03d28197eadeecf and readiness is conditional on resolving the AC-05 implementation gap and identifying the Stage build.
- Product clone `C:\starling`: branch develop; working-tree SHA fdfa72777a2d73b2cdba6d2bdd60ea5535bad75f; captured local upstream SHA 69a98eab3948e98aa78684276be7d37fe30b39ea contains the merged fix; fetch was intentionally not run; ahead 0, behind 1230; clean state is false because pre-existing unrelated and same-file local changes were preserved.
- Product clone `C:\xmleditor\xmleditor`: branch develop; SHA 1f8e3996e40ea4c55cea2e11c52d7ddb6f58b09a; fetch was intentionally not run; ahead 0, behind 1777; clean state is false because pre-existing untracked files were preserved. Its loader posts UPDATEFOLDERPROFILE at `C:\xmleditor\xmleditor\src\aem\loader.ts` L3078-L3086.
- Automation clone `C:\ui_framework\new_editor\guides-ui-tests`: branch develop; SHA bf5dca679ec84f6128d4fe8d9b3b12201e2cc5bb; fetch was intentionally not run; ahead 0, behind 1000; clean state is false because pre-existing test changes were preserved.
- Automation clone `C:\api automation\dxml-it-tests`: branch fix-changebars-nativepdf-lapwing-benchmark; SHA 46709a94fbf357a6277f780202401149c5820054; fetch was intentionally not run; ahead 2, behind 0; clean state is false because pre-existing untracked files were preserved.
- PR #8091 has one final product-code change: 4 added lines and 3 removed lines in FolderProfilesAPI.java. Commit 7f991b0ec1 temporarily added 121 regression-test lines, but commit 0877924f56 removed them before the final squash, so the merged result has no ticket-specific unit tests.
- Figma evidence is not applicable because this change restores a backend authorization decision and does not introduce a new design.

**Code Touched**
- Changed by PR #8091: `C:\starling\core\publish-listener\src\main\java\com\adobe\fmdita\rest\folderprofiles\FolderProfilesAPI.java` in updateFolderProfile. Commit eac2c72512306061d2110b02e03d28197eadeecf moves the isFolderAdmin check before the users-write gate and allows either approved role for a non-global profile.
- Inspected, not changed by the PR: `C:\starling\core\utils\src\main\java\com\adobe\fmdita\common\ACLUtils.java` L314-L356 defines the two role checks.
- Inspected, not changed by the PR: `C:\starling\core\utils\src\main\java\com\adobe\fmdita\folderprofiles\FolderProfileService.java` L1083-L1171 creates the profile group and synchronizes its declared members.
- Inspected, not changed by the PR: `C:\starling\core\publish-listener\src\main\java\com\adobe\fmdita\rest\folderprofiles\FolderProfileServlet.java` L79-L92 routes UPDATEFOLDERPROFILE to the handler using the service resolver.
- Inspected, not changed by the PR: `C:\starling\AEM6.x\repo\jcr_root\libs\fmdita\clientlibs\folderprofiles\js\main.js` L233-L249 and L624-L632 submits the update and selects the success or error message from the HTTP result.

**Lines Changed**
- Final merged diff: FolderProfilesAPI.java has 4 additions and 3 deletions. The effective change computes target-profile membership before the users-write decision and changes the non-global gate from local administrator only to local administrator or delegated Folder Profile admin.
- Reverted before merge: FolderProfilesAPITest.java had 121 ticket-specific test lines in commit 7f991b0ec1; commit 0877924f56 removed all 121 lines together with the six-line mockito-inline dependency change.
- No xmleditor, new-editor, guides-ui-tests, or dxml-it-tests source file is changed by PR #8091.

**Test Scenarios**
- Test data to prepare: on an AEM Guides Cloud Service Stage build containing commit eac2c72512306061d2110b02e03d28197eadeecf, create non-global profile fp-50144-a for `/content/dam/guides-50144`; verify its exact group is `folderprofile-fp-50144-a`; prepare delegated user d-admin, local administrator l-admin, unauthorized user u-author, and candidate user new-admin. Give u-author the Jira-listed non-authorizing groups authors, Contributors, AEM Administrators author Program 167205 Environment 1910614, everyone, AEM Assets Collaborator Users - Service, AEM Sites Content Managers - Service, AEM Administrators - Service, and Analytics Administrators. Withhold exact `administrators` and `folderprofile-fp-50144-a`. Use a full browser refresh plus fresh profile and group reads as persistence oracles.
- Actor matrix - delegated folder-profile admin: grant authors and `folderprofile-fp-50144-a`; withhold exact `administrators`; the Folder Profile assignment auto-adds `folderprofile-fp-50144-a`; use this actor for AC-01, AC-02, and AC-04.
- Actor matrix - local administrator: grant exact `administrators`; do not require `folderprofile-fp-50144-a`; no Folder Profile group is auto-added for setup; use this actor for AC-03.
- Actor matrix - unauthorized user: grant all eight Jira-listed non-authorizing customer groups, including the similarly named Program and Service administrator groups; withhold exact `administrators` and `folderprofile-fp-50144-a`; no Folder Profile group is auto-added; use this actor for AC-05.
- Actor matrix - candidate user: grant authors; start outside exact `administrators` and `folderprofile-fp-50144-a`; AC-01 should auto-add the profile group, and AC-02 should remove it when the candidate is removed from Admin Users.
- P0 [TS-01] [AC-01]: Action: Sign in as d-admin, add new-admin to Admin Users for fp-50144-a, save, and refresh the page. Expected: new-admin remains listed and belongs to `folderprofile-fp-50144-a` after the fresh read.
- P0 [TS-02] [AC-02]: Action: Sign in as d-admin, remove new-admin from Admin Users for fp-50144-a, save, and refresh the page. Expected: new-admin stays absent and no longer belongs to `folderprofile-fp-50144-a` after the fresh read.
- P1 [TS-03] [AC-03]: Action: Sign in as l-admin, add new-admin, verify it, then remove new-admin from fp-50144-a. Expected: both requested changes remain saved after refresh.
- P1 [TS-04] [AC-04]: Action: Sign in as d-admin and try the same Admin Users request against the global profile. Expected: global-profile membership stays unchanged after a fresh read.
- P0 [TS-05] [AC-05]: Action: Sign in as u-author with all eight non-authorizing customer groups and submit an Admin Users change for fp-50144-a. Expected: the request shows an authorization error and a fresh read shows the original membership.
- P1 [TS-06] [AC-01, AC-02]: Action: Repeat add and remove through the direct UPDATEFOLDERPROFILE request, then read the profile and target group from the repository. Expected: the API, profile response, and declared group membership report the same saved user set.
- Implementation gap: TS-05 is expected to fail on the currently inspected PR #8091 result because the unauthorized path still returns HTTP 200 with true and drives the UI success message.

**Known Jira Bugs / Past Similar Tickets**
- Search status: two search_jira_history runs returned no same-mechanism match for Zebra Technologies or other customers. Narrow JQL intents covered the exact false-success error shape and the Folder Profile authorization workflow, so no historical Jira is used as regression authority.
- Evidence-graph status: query_test_evidence_graph was requested in shadow mode but the graph was disabled; it returned no paths and did not influence this plan.

**Regression Areas**
- Re-test the global-profile boundary through the visible UI and a direct request because the widened role condition relies on the existing non-global guard to prevent delegated access from becoming global access.
- Confirm that global-profile authorization is unchanged for both add and remove requests; the non-global permission must not broaden that boundary.
- Re-test local administrators on non-global profiles because widening the condition must preserve the existing broad administrator path while adding the delegated path.
- Re-test profile configuration updates for conditional attributes, templates, and output presets because that sibling update path reuses the moved isFolderAdmin value even though PR #8091 does not intentionally change it.
- Re-test new-profile group creation and Admin User synchronization because authorization depends on the stored group ID and exact declared membership, including add and remove operations.
- Re-test the UI success and error messages against actual HTTP outcomes because the current done callback reports success for the unauthorized no-op response and can hide a failed persistence result.

**Automation Coverage & Gaps**
- Main feature coverage: Not covered - layer: API IT; setup: create the four actors and exact groups in the matrix; poll: re-read profile and group membership after each update; timeout: use the suite's configured HTTP timeout; assert: verify role-specific persistence and denial; cleanup: delete the temporary profile, users, and group; tag: access-control and GUIDES-50144.
- AC-01 and AC-02: Not covered. Recipe - layer: API IT; setup: create a non-global profile, delegated admin outside administrators, existing admin, and candidate user; poll: read profile and declared group after add or remove; timeout: use the configured API timeout; assert: both stores match the requested membership; cleanup: restore membership and delete fixtures; tag: folder-profile-delegated-admin.
- AC-03 and AC-04: Not covered. Recipe - layer: API IT; setup: create a local administrator plus a delegated admin for only the non-global target; poll: read the non-global and global profiles after each request; timeout: use the configured API timeout; assert: local-admin behavior remains valid and global membership is unchanged for the delegated actor; cleanup: restore profiles; tag: folder-profile-role-boundary.
- AC-05: Not covered by PR #8091 or current automation, and the inspected response path is an implementation gap. Recipe - layer: API IT plus one UI check; setup: create u-author outside both allowed groups; poll: read profile membership after the rejected update; timeout: use the configured API and UI timeouts; assert: an authorization error replaces success and membership is unchanged; cleanup: delete the user and profile; tag: folder-profile-unauthorized-denial.
- Existing guides-ui-tests coverage is setup-only: `C:\ui_framework\new_editor\guides-ui-tests\tests\editor_folder_profile\folder_profile.feature` L6-L25 creates a profile and adds its first admin with the default user, then checks only Template-panel visibility for the delegated user.
- Existing dxml-it-tests helpers are reusable but are not role coverage: `C:\api automation\dxml-it-tests\guides-regression\src\main\java\com\adobe\aem\guides\it\regression\utils\FolderProfileUtils.java` L82-L106 can submit Admin Users, while `C:\api automation\dxml-it-tests\guides-regression\src\main\java\com\adobe\aem\guides\it\regression\tests\conditionalprofile\FolderProfileIT.java` L37-L80 calls it only with the local admin client.

**Open Questions**
- OQ-01: Which HTTP status and response body must replace HTTP 200/true for an unauthorized Admin Users update, and will that correction be added to PR #8091 or a linked fix? QA impact: this decides the exact API assertion for AC-05 and whether the current Stage candidate can pass sign-off.
- OQ-02: Which AEM Guides Stage build first contains commit eac2c72512306061d2110b02e03d28197eadeecf? QA impact: this identifies the first build on which all five authorization scenarios must be executed.
