**Understanding From Jira**
- Issue understood: In the AEM Assets Admin UI (Author), the DITA-specific action buttons (View source, Edit topic(s), Preview map, Share UUID link) show up on the wrong assets - they appear on non-DITA files such as PNG images and PDFs, and are missing on genuine DITA files (`.dita`, `.ditamap`) where authors actually need them. The Share UUID link button also produces a broken link (a URL with an empty `uuid=` value) when the asset has no GUID.
- Why it matters: A Critical, customer-reported (SLA3, customer cm-p63456) defect that clutters the Assets Admin UI for non-Guides teams and hides the controls DITA authors need on their own files; confirmed reproducible in the customer's stage environment and by their TAM in production.
- Requested outcome: The DITA-specific buttons must show only on assets DITA authors can actually edit (`.dita`, `.ditamap`, etc.), stay hidden on non-DITA assets (images, PDFs, PPTX, etc.), and the Share UUID link button must only appear (or must be disabled) when the asset actually has a GUID - never producing an empty-UUID link. A related cosmetic font-fallback issue in preview is out of scope for this plan (no code evidence gathered for it).
- Lifecycle understood as: Implementation Review - Jira records a fix PR (`starling#8095`) with a stated root cause and a code-review comment already flagging that the fix's new render condition checks the wrong UUID property, so the acceptance contract must account for a fix that exists but is not yet correct.
- Evidence boundary: Evidence mode: degraded. Live Jira issue and all 7 comments fetched via the backend Jira client (Jira MCP OAuth unavailable this session); both attachments downloaded and visually inspected - one shows the button menu on a non-DITA PNG, the other on a non-DITA PDF, both confirming the reported symptom. The Starling clone (`C:\starling`, branch develop) was inspected read-only for the current (pre-fix) button-visibility code, the Share UUID click handler, and the URL-resolving servlet. PR `#8095` itself - the new render condition and its property fix - is not present in the local clone and GitHub MCP is not connected this session, so that diff could not be inspected; any claim about the fix's correctness stays Proposed. Product documentation RAG returned only DITA publishing/output-preset pages, not Admin UI button behaviour, and was excluded as not supportive.

**Acceptance Criteria**
- AC-01: View source is visible only for DITA source files (`.dita`, `.xml`, `.ditaval`, `.ditamap`) selected in the asset-details view, and is hidden for any non-DITA asset.
- AC-02: The Edit action (Edit topic for a DITA topic, Edit topics for a DITA map) is visible only for the matching DITA type in the asset-details view, and is hidden for non-DITA assets.
- AC-03: Preview map is visible only for a DITA map (`.ditamap`) and is hidden for DITA topics and for all non-DITA assets.
- AC-04: Share UUID link is visible only for an asset that has a GUID (maps, topics, and supported image files); it is hidden or disabled for assets without a GUID and for unsupported types, and never produces a URL with an empty uuid value.
- AC-05: A non-DITA asset such as a PNG or PDF hides View source, Edit topic(s), and Preview map, while Share UUID link appears only if the asset is a supported image that has a GUID - a non-DITA asset does not blanket-hide every action.

**Expected Behaviour**
- From Jira (verified): the correct behaviour is that DITA-editing buttons appear only for `.dita`/`.ditamap` assets and Share UUID link appears only when a GUID exists; the incorrect current behaviour is the reverse for the DITA-editing buttons and an ungated Share UUID link.
- From product clone (verified, current code): in the single-asset details/Quick-View path, `alltopics` and `allDitamaps` are initialized `true` and are only recomputed from the actual asset extension inside a loop that runs only when a grid multi-selection exists (`selections.length !== 0`); in the details view that loop never runs, so `View source`/`Preview map` default to visible regardless of asset type. `Edit topic(s)` visibility is set only inside that same loop, so it is never explicitly turned on for a DITA asset opened directly - matching the reported "missing on DITA files" symptom.
- From product clone (verified, current code): the Share UUID link click handler (`uuid-resolver.js#getUuid`) builds the `/bin/fmdita/urlresolver` URL with the resolved UUID and copies it to the clipboard with no check that a UUID was actually resolved, confirming the reported empty-UUID URL is currently unguarded.
- From product clone (verified, current code): `UuidURLServlet.doGet` logs a 432 error when the `uuid` request parameter is absent but does not return afterward, so it proceeds to resolve a null/blank UUID - a related defect independent of the button-visibility symptom.
- Refinement of developer root cause (flagged as Open Question, not silently substituted): the Jira fix-ready comment describes the cause as "inverted asset-type discrimination"; the code instead shows an uninitialized-default-plus-unset-DOM-state bug specific to the single-asset (non-grid) rendering path. The observable symptom matches; the precise mechanism is narrower. This needs the PR author's confirmation once the PR diff is reviewable.
- Whether the current Share UUID link button has ANY existing visibility gate is Unknown from current evidence - no hide/show reference to it was found in the inspected clientlib, so it may be unconditionally rendered in markup today.
- The reported preview font fallback to Times New Roman is Unknown from current evidence - no code path for font loading was inspected for this plan.

**Scope From Git**
- Lifecycle stage is Implementation Review; readiness target is review-ready, not yet QA-sign-off-ready, because the fix PR has an open, unresolved code-review finding.
- Issue source: live Jira `GUIDES-47043` (Critical, component Authoring, labels Not_Automated/Redhat/Triaged/UAC_Check/fluffyjaws-investigation/sla3) fetched via the backend Jira client fallback; a development link to `starling#8095` (`https://git.corp.adobe.com/AdobeStarling/starling/pull/8095`) is recorded in a comment, not in Jira's own development-link metadata.
- Backend clone `C:\starling`: branch develop, SHA `fdfa72777a`, inspected read-only; fetch/ahead/behind not run this pass, so the worktree state versus upstream is unknown and clean/dirty state was not checked - all clone-derived claims are provisional and should be re-confirmed before implementation-review sign-off.
- PR `#8095` itself was not inspected: it is not merged to this local clone (confirmed absent - no `isditaasset` construct anywhere in the repository) and GitHub MCP is not connected this session, so the actual diff, including the disputed UUID-property check, could not be read.
- Automation clone discovery was not run this pass; this is an evidence gap for `Automation Coverage & Gaps` below, not a blocker for the acceptance contract.
- Figma design evidence is not applicable; this is existing button-visibility logic, not a new UI design.

**Code Touched**
- Current implementation implicated: `C:\starling\AEM6.x\repo\jcr_root\libs\fmdita\clientlibs\clientlib-damview\dam-buttons.js` - function `handleNavActions` (lines 571-707) computes DITA-button visibility; the single-asset details path (lines 665-699) relies on `alltopics`/`allDitamaps` flags that default `true` and are only corrected inside the grid multi-select loop (lines 609-663).
- Current implementation implicated: `C:\starling\AEM6.x\repo\jcr_root\libs\fmdita\clientlibs\clientlib-damview\uuid-resolver\uuid-resolver.js` - function `getUuid` (lines 5-16) and the `DETAILS_SHARE_UUID` click handler (lines 24-28) build and copy the resolver URL with no UUID-presence gate.
- Current implementation implicated: `C:\starling\core\utils\src\main\java\com\adobe\fmdita\uuid\concrete\UuidURLServlet.java` - `doGet` (lines 41-71) is missing a `return` after the 432 error path (lines 45-49).
- Potential code impact (inference, PR diff not inspected): a new Sling render condition referenced in Jira comments as checking `isditaasset` and a UUID property, intended to gate button/link visibility server-side; its exact location in the codebase is unknown because it is not merged locally.

**Lines Changed**
- Draft blocker: implementation diff not inspected - PR `#8095` is referenced in Jira but is not present in the local clone and GitHub MCP is unavailable this session, so added/deleted line counts and hunks cannot be reported.

**Test Scenarios**
- Setup and test data: at least one genuine DITA topic file (`.dita`) and one DITA map (`.ditamap`) with an assigned GUID; at least one non-DITA asset with a GUID (e.g. a PNG image) and one without a GUID (a freshly uploaded, unprocessed asset); a mixed multi-selection containing both a DITA and a non-DITA asset. Oracles: the visible/hidden state of each toolbar button (UI), the exact URL copied by Share UUID link (clipboard/network), and the HTTP status/body returned by `/bin/fmdita/urlresolver` for a missing `uuid` parameter.
- P0 [AC-01]: Action: open the asset-details view for a `.dita` topic, then for a PNG image. Expected: View source is visible for the DITA topic and hidden for the PNG.
- P0 [AC-02]: Action: open the asset-details view for a `.dita` topic, then a `.ditamap`, then a PNG. Expected: Edit topic shows for the topic, Edit topics shows for the map, and neither shows for the PNG.
- P0 [AC-03]: Action: open the asset-details view for a `.ditamap`, then a `.dita` topic, then a PNG. Expected: Preview map shows only for the map and is hidden for the topic and the PNG.
- P0 [AC-04]: Action: open the asset-details view for an image that has a GUID, then for an asset with no GUID, then for a PDF. Expected: Share UUID link is visible for the GUID-bearing image, and hidden or disabled (no empty-uuid URL) for the no-GUID asset and the unsupported PDF.
- P0 [AC-05]: Action: open the asset-details view for the two attached fixtures - a PNG and a PDF. Expected: View source, Edit topic(s), and Preview map are all hidden; Share UUID link appears only if the asset is a supported image with a GUID, confirming non-DITA does not blanket-hide every action.
- Implementation verification (not an acceptance criterion): call `/bin/fmdita/urlresolver` with the `uuid` parameter omitted and confirm it returns the 432 error and does not continue to resolve a null uuid. This is the missing-`return` servlet finding (`UuidURLServlet.doGet`), dispositioned as an implementation oracle / secondary defect (thread T4), not a customer-facing product contract.

**Known Jira Bugs / Past Similar Tickets**
- Search status: `search_jira_history` was queried twice on the indexed `jira_qa` corpus (component `Authoring`, once attempting a same-customer filter which the corpus does not have data for, once cross-customer); three hits returned both times. Live-Jira JQL intents were also framed for this search: by exact error text (`text ~ "empty uuid" OR text ~ "urlresolver"`), by workflow (`component = Authoring AND text ~ "asset type" AND text ~ "button"`), and by code area (`text ~ "handleNavActions" OR text ~ "isditaasset"`); none of these narrower JQL forms were run live this pass (indexed search only), which is recorded as a gap, not a completed search. Two hits were excluded and one kept as a weak/adjacent regression signal, not a same-defect-class match.
- GUIDES-2461 - "Manage Tags menu item in Assets UI does not work for DITA content." Similarity: adjacent, not same-defect-class - both are DITA-specific controls in the Assets Admin UI misbehaving relative to asset type, but the failure mechanism (a menu action failing to run) differs from this ticket's visibility-toggle logic. Status: Closed. Resolution: not available in current evidence. Affected version: not available in current evidence. Fix version: not available in current evidence. RCA: not available in current evidence. Test evidence: not available in current evidence. Impact: confirms this general UI area (DITA-specific Assets-Admin controls) has a history of asset-type-conditional bugs; kept only as area context, not as a reusable oracle.
- Excluded as different-mechanism: GUIDES-12188 (a DITA-file namespace/filename validation error - unrelated to button visibility) and GUIDES-36662 (an icon-consistency requirement between the Select Path/Asset picker and the AEM Repository browser - a different UI surface with no verified shared code path to this fix; kept only as an unverified regression note below, not as an acceptance criterion).

**Regression Areas**
- Highest priority: re-run the Assets grid multi-select toolbar (not just the single-asset details view) for every button this fix touches, because `handleNavActions` computes both paths from the same function and a fix aimed at the details-view default could change the already-working multi-select behaviour.
- Worth a spot-check, not a required assertion: the Select Path/Asset picker and AEM Repository browser icon display for DITA assets, because GUIDES-36662 shows this general UI area has a history of icon-consistency issues - no code inspection confirmed this fix's discrimination logic actually feeds that picker, so this is a caution, not a verified regression path.
- Re-test the `/bin/fmdita/urlresolver` servlet's other `target` values (`EDITOR`, `PREVIEW`, default) with a valid UUID, because the fix for the missing-`uuid` case should not change the resolved-URL behaviour for the already-working paths.
- Re-verify Native PDF and other output presets are unaffected, since this is purely an Admin-UI/DAM-view change and should have no publishing-pipeline impact; call this out explicitly so QA does not need to re-run the full output-preset matrix.

**Automation Coverage & Gaps**
- Main feature coverage: automation clone discovery (`guides-ui-tests`, `dxml-it-tests`) was not run this pass, so existing coverage could not be confirmed or ruled out; this is an evidence gap to close before QA sign-off, not a statement that no coverage exists anywhere. See the per-criterion verdicts below.
- AC-01: Not covered. Recipe - layer: UI Selenium/Behave; setup: import a `.dita` topic and a PNG; poll: open the asset-details view for each and wait for the toolbar to render; timeout: use the existing page-load timeout helper; assert: View source visible for the DITA topic and hidden for the PNG via CSS display state; cleanup: delete the imported fixtures; tag: assets-admin-ui regression suite.
- AC-02: Not covered. Recipe - layer: UI Selenium/Behave; setup: import a `.dita` topic, a `.ditamap`, and a PNG; poll: open the asset-details view for each and wait for the toolbar to render; timeout: use the existing page-load timeout helper; assert: Edit topic for the topic, Edit topics for the map, neither for the PNG; cleanup: delete the imported fixtures; tag: assets-admin-ui regression suite.
- AC-03: Not covered. Recipe - layer: UI Selenium/Behave; setup: import a `.ditamap`, a `.dita` topic, and a PNG; poll: open the asset-details view for each and wait for the toolbar to render; timeout: use the existing page-load timeout helper; assert: Preview map visible only for the map, hidden for topic and PNG; cleanup: delete the imported fixtures; tag: assets-admin-ui regression suite.
- AC-04: Not covered. Recipe - layer: UI Selenium/Behave; setup: import an image with a GUID, an asset with no GUID, and a PDF; poll: open the asset-details view for each and wait for the toolbar to render; timeout: use the existing page-load/click timeout helper; assert: Share UUID link visible for the GUID image, hidden/disabled with no empty-`uuid` URL for the no-GUID asset and the PDF; cleanup: delete the imported fixtures; tag: assets-admin-ui regression suite.
- AC-05: Not covered. Recipe - layer: UI Selenium/Behave; setup: import the two attached fixtures (a PNG and a PDF); poll: open the asset-details view for each and wait for the toolbar to render; timeout: use the existing page-load timeout helper; assert: View source, Edit topic(s), Preview map all hidden, Share UUID link present only for a supported GUID-bearing image; cleanup: delete the imported fixtures; tag: assets-admin-ui regression suite.
- Implementation verification (servlet missing-return, thread T4): Not covered. Recipe - layer: API IT (`dxml-it-tests` style); setup: a valid authenticated session; poll: call `/bin/fmdita/urlresolver` with the `uuid` parameter omitted; timeout: standard HTTP request timeout; assert: status 432 and no further resolver processing (no 500/NPE); cleanup: none required; tag: urlresolver regression suite. This is an implementation oracle, not a product acceptance criterion.

**Open Questions**
- OQ-1: Does the PR author agree that the current bug in the single-asset details path is an uninitialized-default-plus-unset-DOM-state issue rather than a simple inverted condition? QA impact: if confirmed, the fix scenario should specifically exercise the details/Quick-View path opened directly (not via a prior grid selection), since that is the path this analysis found broken; if the PR fixes only the multi-select path, the details-view symptom could remain.
- OQ-2: Is the `isditaasset` render condition's UUID-property check (flagged in the PR review as checking `jcr:content/fm:uuid` instead of `fmUuid`) already corrected in a newer PR revision, or still open? QA impact: until confirmed fixed, AC-03 (Share UUID link gating) cannot be verified end-to-end and must be re-tested against the corrected property once the PR updates, not assumed to already work.
- OQ-3: Does the current Share UUID link button have any existing visibility gate today, or is it unconditionally present in markup? QA impact: if it is currently ungated, the fix must add the gate from scratch (broader change) rather than correcting an existing but wrong condition; this changes what "no regression" means for assets that previously always showed the link.
- OQ-4: Is the reported font fallback to Times New Roman in scope for this ticket, or a separate cosmetic issue? QA impact: if in scope, a rendering/font-loading test scenario is needed; if out of scope, it should be tracked separately so this fix is not blocked on it.
- OQ-5: Scope alignment - the reported Jira covers four toolbar actions (View source, Edit topic(s), Add/Edit topic, Share UUID link) plus a font issue, but the current fix (PR starling#8095) evidences only Share UUID link gating (and a reviewer flagged even that checks the wrong UUID property). Does the fix also correct the inverted visibility of View source, Edit, and Preview map, or only Share UUID link? QA impact: alignment is PARTIAL - if the fix is Share-UUID-only, AC-01 through AC-03 remain unverified and the Jira is not fully solved, so those must not be signed off on this PR; if it covers all four, they become verifiable now.
- OQ-6: Is multi-selection supported for these actions, and what is the eligibility policy across a mixed DITA + non-DITA selection (all-must-support, primary-item-controls, or disabled for multi-select)? QA impact: the selection policy is unresolved in current evidence, so no mixed-selection acceptance criterion is asserted; once product/code evidence establishes the policy, add the matching scenario - until then it stays an open question rather than an invented AC.
