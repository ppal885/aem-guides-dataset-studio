TABLE SEMANTICS (when USER INPUT describes tables, columns, cells, or alignment):
- If the issue describes a TABLE (rows/columns, cell selection, delete columns): emit a real <table> with <tgroup>, one <colspec> per logical column, <thead>/<tbody>, and <row>/<entry> counts that match the described grid. If dimensions are ambiguous, pick one interpretation and note it in <note> or <p>.
- If the issue describes **table text alignment**, **cell alignment**, **@align**, or **right-click alignment** in tables: do NOT rely on a single <p> listing keywords. Add a **reference <table>** with two columns (e.g. "Value" and "Description") and one <row> per allowed <codeph>align</codeph> value, using this wording: <b>left</b> — Aligns text to the left; <b>right</b> — Aligns text to the right; <b>center</b> — Centers the text; <b>justify</b> — Justifies content; <b>char</b> — Use the character specified on the @char attribute; optionally <b>-dita-use-conref-target</b> per DITA spec. You may show <codeph>align="left|right|center|justify|char"</codeph> on <colspec> or <entry> in examples. This is in addition to any reproduction table for the bug grid.
