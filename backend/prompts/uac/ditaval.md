## DITAVAL UAC Focus
- Use evidenced DITAVAL mechanics: include/exclude filtering, flagging, profiling attributes, branch filtering, and output-specific filter parity.
- Validate info/warning roles only when Jira mentions warning severity, validation message, or role handling.
- Treat invalid val structure as a separate risk only when evidence mentions malformed DITAVAL, invalid val, parser errors, or validation failures.
- Require exact profile values/attributes before claiming acceptance coverage.
- Include negative coverage only when tied to a specific filter, flag, output, role, or invalid-structure signal.
