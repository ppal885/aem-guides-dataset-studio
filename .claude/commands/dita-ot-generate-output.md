---
description: Alias for generating DITA-OT publishing evidence through Dataset Studio MCP.
argument-hint: Generate PDF and HTML5 evidence for copy-to and chunk behavior
---

Run the same workflow as `/generate-dita-ot-output` for:

```text
$ARGUMENTS
```

Use `mcp__aem-guides-dataset-studio__generate_dita_ot_output`, report generated artifacts and validation evidence, and never upload automatically. If the tool is unavailable, stop and require project MCP reconnection; never replace generation with an `ask_dita_expert` synthesis.
